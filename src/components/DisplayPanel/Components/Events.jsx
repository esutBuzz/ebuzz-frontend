import styled from "styled-components"

const EventsDisplay = styled.main`
    position: relative;
    width: 100%;
    height: 100%;
`

export default function Events() {
  return (
    <EventsDisplay>
        Events
    </EventsDisplay>
  )
}
