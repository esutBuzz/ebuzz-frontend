import styled from "styled-components"

const ProfileDisplay = styled.main`
    position: relative;
    width: 100%;
    height: 100%;
`

export default function Profile() {
  
  return (
    <ProfileDisplay>
        Profile fuzzy
    </ProfileDisplay>
  )
}
